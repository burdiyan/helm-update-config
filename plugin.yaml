name: "update-config"
version: "0.2.0"
usage: "update config of a running release"
description: Update configuration values or templates of a running release.
ignoreFlags: false
useTunnel: true
command: "$HELM_PLUGIN_DIR/bin/helm-update-config"
hooks:
  install: "$HELM_PLUGIN_DIR/install.sh"
